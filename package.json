{
  "name": "invisible-sun",
  "version": "0.1.0",
  "description": "A system for the Invisible Sun RPG by Monte Cook Games.",
  "scripts": {
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "test": "npm run lint && node scripts/check.mjs",
    "smoke": "node scripts/smoke.mjs",
    "dist": "node scripts/dist.mjs",
    "packs:build": "node scripts/build_compendia.js",
    "packs:compile": "node scripts/compile_packs.js",
    "packs:verify": "node scripts/verify_packs.js",
    "packs": "npm run packs:build && npm run packs:compile && npm run packs:verify",
    "shot": "node scripts/shoot.mjs"
  },
  "keywords": [],
  "author": "DeviousDVS",
  "license": "MIT",
  "type": "commonjs",
  "devDependencies": {
    "@foundryvtt/foundryvtt-cli": "^3.0.4",
    "eslint": "^9.39.5",
    "playwright": "^1.62.1"
  }
}
